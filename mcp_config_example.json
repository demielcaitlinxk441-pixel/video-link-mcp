{
  "mcpServers": {
    "video-link-analyzer": {
      "command": "REPLACE_WITH_VENV_PYTHON_PATH",
      "args": ["REPLACE_WITH_SERVER_PY_PATH"],
      "env": {
        "WECHAT_CHANNELS_ALLOW_PUBLIC_WORKER": "false"
      }
    }
  }
}
